# Python
venv/
__pycache__/
*.pyc

# Playwright MCP
.playwright-mcp/

# Trained models (large, regenerable)
data/models/*.mlpackage/
data/models/*.mlmodel
data/models/*.pt
data/models/*.onnx
data/models/hearing_aid_yolo*/
data/yolo_dataset/
data/yolo_runs/

# Ultralytics auto-downloads pretrained base weights to repo root
yolo11n.pt
yolov*.pt
runs/

# Firebase deploy cache (local-only)
.firebase/

# Local hearing-aid capture data — large binaries, kept local (accidentally committed in #83, untracked in this commit)
data/photos_nick_aids/
